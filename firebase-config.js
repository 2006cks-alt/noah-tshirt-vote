// Paste the firebaseConfig object Firebase gives you when you register a
// Web App (Project settings → General → Your apps → </> Web).
// This is safe to commit / ship in client code — it's not a secret, access
// control is enforced by firestore.rules and storage.rules instead.
export const firebaseConfig = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME",
};
